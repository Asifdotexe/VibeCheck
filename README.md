# VibeCheck
A lightweight Streamlit app that uses a pre-trained DistilBERT model to analyze sentiment (Positive / Negative / Neutral) in any text, with confidence scores. Built with Hugging Face Transformers and PyTorch.
